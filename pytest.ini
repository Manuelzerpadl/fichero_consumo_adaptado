[pytest]
markers =
    integration: tests que requieren conexión real a servicios externos (API, DB, etc.)
    unit: tests rápidos, sin dependencias externas, con mocks
    slow: tests más lentos o pesados (opcional)
